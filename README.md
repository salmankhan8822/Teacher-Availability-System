# Teacher-Availability-System

This project is a simple web-based application that displays class information such as subject name, teacher name, and teacher availability. Users can click a button to view the next class, and the background color changes according to the teacher's availability (green = available, red = not available).

## 🚀 Features

- Shows subject name
- Shows teacher name
- Shows teacher availability (Available / Not Available)
- Background color changes according to status
- Next class button to switch between classes
- Restart option when the last class is reached
- Dynamic UI elements created using JavaScript

## 🧠 Technologies Used

- HTML  
- CSS  
- JavaScript  

## 📌 How It Works

1. The project contains an array of classes.
2. Each object includes:  
   - name  
   - teacherName  
   - isVisible` (true = available, false = not available)
3. When the button is clicked:
   - It displays class details.
   - Shows green/red status.
   - Moves to the next class using index.
4. After the last class, it shows a "restart" option.

## 📂 Project Structure

